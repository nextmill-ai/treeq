# Cloudflare Pages project config.
# `npx wrangler pages deploy public` reads this for the project name.
# The `pages_build_output_dir` tells Pages where the static frontend lives.

name = "spartan-cut-estimator"
pages_build_output_dir = "./public"
compatibility_date = "2026-05-01"
